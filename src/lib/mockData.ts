import { FuturesMarket, Position, Order, Orderbook, Trade } from '@/types';

export const mockMarkets:   FuturesMarket[] = [];
export const mockPositions: Position[]      = [];
export const mockOrders:    Order[]         = [];
export const mockOrderbook: Orderbook       = { asks: [], bids: [] };
export const mockTrades:    Trade[]         = [];
